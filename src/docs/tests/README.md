# Tests Docs Index

## Scope
Summarizes test layout and high-value scenario coverage in `tests/`.

## Navigation
- `tests/unit/` unit-level behavior and guardrails
- `tests/integration/` end-to-end and cross-module workflows

## Maintenance Rules
- Keep this index aligned when adding new critical test domains.
- Prefer listing behavior categories over individual test files unless necessary.

## Related Code
- `tests/`
